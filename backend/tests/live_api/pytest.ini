# pytest.ini

[pytest]
markers =
    unit: Unit tests (fast, with mocks)
    integration: Integration tests (slower, with real DB)
    live_api: Live API tests (expensive, requires API keys)
    slow: Slow running tests

# Default: skip live API tests (expensive)
addopts = -v --tb=short --strict-markers -m "not live_api"